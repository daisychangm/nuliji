import localFont from 'next/font/local'

export const objectiveFont = localFont({ 
    src: [
        {
            path: "fonts/Objective.ttf",
        }
    ],
    variable: "--font-objective",
})

export const viga = localFont({ 
    src: [
        {
            path: "fonts/Viga-Regular.ttf",
        }
    ],
    variable: "--font-viga",
})